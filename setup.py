from setuptools import setup, find_packages
setup(name="memoryweave", version="0.1.0", description="Agent Memory System — persistent self-organizing searchable",
      packages=find_packages(), python_requires=">=3.9")
